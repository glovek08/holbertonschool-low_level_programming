#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: 0 if correct.
 */
int main(void)
{
	/* writes a string to the std output */
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
